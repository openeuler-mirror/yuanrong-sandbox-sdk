# openYuanrong Sandbox SDK for Go

Planned language SDK directory. Keep future Go module files, examples, and tests
here, for example:

```text
go/
  go.mod
  yr/ or sandbox/
  examples/
  tests/
```

The Go SDK should follow the repository-wide concepts in the root README and use
the same environment variables and route semantics.
