# openYuanrong Sandbox SDK for Rust

Planned language SDK directory. Keep future Cargo crate files, examples, and
tests here, for example:

```text
rust/
  Cargo.toml
  src/
  examples/
  tests/
```

The Rust SDK should follow the repository-wide concepts in the root README and
use the same environment variables and route semantics.
